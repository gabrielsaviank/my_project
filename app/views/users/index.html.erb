<p id="notice"><%= notice %></p>

<h1>X-Solar Tech</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Cpf</th>
      <th>Telefone</th>
      <th>Email</th>
      <th>Cep</th>
      <th>Cidade</th>
      <th>Estado</th>
      <th>Bairro</th>
      <th>Rua</th>
      <th>Número</th>
      <th>Complemento</th>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.cpd %></td>
        <td><%= user.phone %></td>
        <td><%= user.email %></td>
        <td><%= user.zip %></td>
        <td><%= user.city %></td>
        <td><%= user.state %></td>
        <td><%= user.borough %></td>
        <td><%= user.street %></td>
        <td><%= user.number %></td>
        <td><%= user.comp %></td>
        <td><%= user.tipo %></td>
        <td><%= link_to 'Mostrar', user %></td>
        <td><%= link_to 'Editar', edit_user_path(user) %></td>
        <td><%= link_to 'Excluir', user, method: :delete, data: { confirm: 'Voce deseja realmente excluir o registro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Cliente', new_user_path %>
